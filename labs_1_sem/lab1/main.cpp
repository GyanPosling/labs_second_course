#include "performLab.h"
#include <windows.h>

// g++ main.cpp performLab.cpp StringWorker.cpp -o main.exe

int main() {
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
    performTask();
    return 0;
}